type Query {
    products : [Product]
    sellers(name: String) : [ProductSeller]
}

scalar DateTime

type Product {
    id: ID!
    created: DateTime!
    name: String!
    description: String!
}

type ProductSeller {
    id: ID!
    created: DateTime!
    name: String!
}